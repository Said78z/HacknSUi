{
  "name": "sui-hackathon-project",
  "version": "1.0.0",
  "description": "SUI Hackathon Project - Full Stack dApp",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev:frontend\" \"npm run dev:backend\"",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd backend && npm run dev",
    "build": "npm run build:frontend && npm run build:backend",
    "build:frontend": "cd frontend && npm run build",
    "build:backend": "cd backend && npm run build",
    "deploy:testnet": "cd move && sui client publish --gas-budget 100000000",
    "deploy:devnet": "cd move && sui client publish --gas-budget 100000000 --network devnet"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
